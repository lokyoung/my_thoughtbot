Dashboard
<%= form_for @dashboard.new_text_shout do |form| %>
  <%= form.text_field :body, placeholder: 'shouts here' %>
  <%= form.submit 'Shout!' %>
<% end %>

<%= form_for @dashboard.new_photo_shout do |form| %>
  <%= form.file_field :image %>
  <%= form.submit 'Shout!' %>
<% end %>

<!--这里需要一个shouts的局部视图-->
<%= render @dashboard.timeline %>

